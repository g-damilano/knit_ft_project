[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "myproject"
version = "0.1.0"
description = "HOG-guided FFT knit spacing estimator"
requires-python = ">=3.10"
dependencies = [
    "numpy",
    "matplotlib",
    "opencv-python",
]
